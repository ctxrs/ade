{
  "name": "ctx-install-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test src/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.67.0"
  }
}
